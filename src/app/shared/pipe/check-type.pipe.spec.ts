import { CheckTypePipe } from './check-type.pipe';

describe('CheckTypePipe', () => {
  it('create an instance', () => {
    const pipe = new CheckTypePipe();
    expect(pipe).toBeTruthy();
  });
});
